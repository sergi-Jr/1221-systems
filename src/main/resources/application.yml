spring:
  application:
  name: 1221-systems
  profiles:
    default: dev
  jpa:
    generate-ddl: true
    show-sql: true
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true
  output:
    ansi:
      enabled: always

user:
  floatPrecision: 0.01f
  suggestion:
    slim: 0.75f
    main: 1
    gain: 1.25f